<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "<span class=\"fa fa-bar-chart\"></span> WRULA".html_safe, root_url, class: "navbar-brand" %>
    </div>

    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Home", root_url %></li>
          <%= render "shared/admin_menu" %>
          <%= render "shared/profile_menu" %>
          <li><%= link_to t("devise.buttons.signout"), destroy_user_session_path, method: :delete %></li>
        <% else %>

          <li><%= link_to t("devise.buttons.signup"), new_user_registration_path %></li>
          <li><%= link_to t("devise.buttons.signin"), new_user_session_path %></li>
        <% end %>
      </ul>
    </div>

  </div>
</nav>